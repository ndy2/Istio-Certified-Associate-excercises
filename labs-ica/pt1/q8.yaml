apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: notification
spec:
  hosts:
    - rnotification-service
  http:
    - route:
        - destination:
            host: notification-service
            subset: v1
      fault:
        delay:
          percentage:
            value: 100
          fixedDelay: 3s
